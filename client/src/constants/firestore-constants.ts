export enum FirestoreSpecialOperations {
  Increment = 'increment', // => number
  ServerTimestamp = 'serverTimestamp', // => Timestamp
  DeleteField = 'deleteField', // => null
  ArrayUnion = 'arrayUnion', // => T[]
}
